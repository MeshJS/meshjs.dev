import Link from "fumadocs-core/link";
import {
  ArrowRight,
  Code,
  Zap,
  Shield,
  Wallet,
  Database,
  Blocks,
  Sparkles,
  Rocket,
  Users,
  Github,
  Terminal,
  FileCode,
  Lock,
  ShoppingCart,
  Gift,
  ArrowLeftRight,
  Coins,
  Layers,
} from "lucide-react";
import { cn } from "@/lib/cn";
import { buttonVariants } from "@/components/ui/button";
import { iconResolver } from "@/lib/iconResolver";
import { FeatureSplit } from "@/components/home/FeatureSplit";
import { Cards } from "fumadocs-ui/components/card";
import {
  Card,
  CardHeader,
  CardDescription,
  CardTitle,
} from "@/components/ui/card";
import {
  BoltIcon,
  PaintBrushIcon,
  RocketLaunchIcon,
  WalletIcon,
  WrenchScrewdriverIcon,
  ComputerDesktopIcon,
  DocumentCheckIcon,
  CloudIcon,
} from "@heroicons/react/24/solid";
import {linksApi} from "@/data/links-api";

<main className="min-h-screen">
  {/* ═══════════════ HERO ═══════════════ */}
  <section className="py-20 lg:py-28">
    <div className="container mx-auto px-4 sm:px-6 lg:px-8">
      <div className="text-center max-w-3xl mx-auto">

        <div className="inline-flex items-center mb-4 px-3 py-1 rounded-full border border-border text-sm font-medium">
          <span className="size-1.5 rounded-full mr-2 bg-green-500"></span>
          <Link href="/ai">Mesh AI features help you ship faster</Link>
        </div>

        <h1 className="font-heading font-extrabold text-5xl sm:text-6xl lg:text-7xl mb-6 leading-tight">
          MeshJS: The Cardano TypeScript SDK for Modern dApp Development
        </h1>

        <div className="text-lg sm:text-xl mb-8 max-w-2xl mx-auto leading-relaxed text-muted-foreground">
          MeshJS is the leading open-source Cardano TypeScript SDK, giving you a transaction builder, wallet connectors, React components, and smart contract tools to start building dApps in minutes.
        </div>

        <div className="mb-8 inline-flex items-center gap-3 bg-fd-card border border-border rounded-xl px-6 py-3 font-mono text-sm">
          <Terminal className="w-4 h-4 text-muted-foreground" />
          <span>npm install @meshsdk/core</span>
        </div>

        <div className="flex flex-col sm:flex-row gap-4 justify-center items-center">
          <Link
            href="/guides"
            className={cn(
              buttonVariants({ size: 'lg', color: 'primary' }),
              'rounded-xl px-10 py-4 text-lg font-semibold flex items-center justify-center gap-2'
            )}
          >
            Get Started
            <ArrowRight className="w-5 h-5" />
          </Link>
          <Link
            href="https://github.com/MeshJS/mesh"
            className={cn(
              buttonVariants({ size: 'lg', color: 'secondary' }),
              'rounded-xl px-10 py-4 text-lg font-semibold flex items-center justify-center gap-2'
            )}
          >
            <Github className="w-5 h-5" />
            View on GitHub
          </Link>
        </div>

        <div className="mt-12 flex flex-wrap justify-center gap-8 text-sm text-muted-foreground">
          <span className="inline-flex items-center">
            <Shield className="w-4 h-4 mr-1" />
            Less than 60kB
          </span>
          <span className="inline-flex items-center">
            <Sparkles className="w-4 h-4 mr-1" />
            AI Integration
          </span>
          <span className="inline-flex items-center">
            <Rocket className="w-4 h-4 mr-1" />
            Production Ready
          </span>
          <span className="inline-flex items-center">
            <Users className="w-4 h-4 mr-1" />
            34K+ Monthly Downloads
          </span>
        </div>
      </div>
    </div>
  </section>

  {/* ═══════════════ FEATURE CARDS ═══════════════ */}
  <section className="py-20 lg:py-28">
    <div className="container mx-auto px-4 sm:px-6 lg:px-8">
      <div className="text-center mb-16">
        <h2 className="font-heading font-bold text-3xl lg:text-4xl mb-4">Everything You Need to Build Cardano dApps</h2>
        <div className="text-xl max-w-2xl mx-auto text-muted-foreground">
          Wallets, transaction builders, data utilities, smart contract libraries, and blockchain providers in one SDK
        </div>
      </div>

      <div className="grid md:grid-cols-2 lg:grid-cols-3 gap-6 items-stretch">
        {linksApi.map((item) => (
          <Link key={item.title} href={item.link}>
            <Card className="h-full hover:border-foreground/20 transition-colors duration-150 px-4 py-8">
              <CardHeader>
              <div className="w-12 h-12 bg-muted rounded-lg flex items-center justify-center mb-4">
                {typeof item.icon !== 'string' ? (
                  <item.icon className="w-6 h-6" />
                  ) :
                  <div>
                    {iconResolver(item.icon, "w-6 h-6 object-contain")}
                  </div>
                }
              </div>
              <CardTitle className="font-heading">{item.title}</CardTitle>
              <CardDescription>{item.desc}</CardDescription>
              </CardHeader>
            </Card>
          </Link>
        ))}
      </div>
    </div>
  </section>

  {/* ═══════════════ WHY MESH ═══════════════ */}
  <section className="py-20 lg:py-28">
    <div className="container mx-auto px-4 sm:px-6 lg:px-8">
      <div className="text-center mb-16">
        <h2 className="font-heading font-bold text-3xl lg:text-4xl mb-4">Why Developers Pick Mesh</h2>
        <div className="text-xl max-w-2xl mx-auto text-muted-foreground">
          Built by developers who ship Cardano apps every day
        </div>
      </div>

      <div className="grid md:grid-cols-3 gap-8">
        <div className="text-center p-6">
          <div className="w-14 h-14 bg-muted rounded-xl flex items-center justify-center mx-auto mb-5">
            <FileCode className="w-7 h-7" />
          </div>
          <h3 className="font-heading font-bold text-xl mb-3">TypeScript-First</h3>
          <div className="text-muted-foreground">
            Full type definitions for every API. Catch errors at compile time, get autocomplete in your editor, and skip the guesswork. No separate type packages needed.
          </div>
        </div>

        <div className="text-center p-6">
          <div className="w-14 h-14 bg-muted rounded-xl flex items-center justify-center mx-auto mb-5">
            <Zap className="w-7 h-7" />
          </div>
          <h3 className="font-heading font-bold text-xl mb-3">Small and Fast</h3>
          <div className="text-muted-foreground">
            The core package is under 60kB. Tree-shakeable imports keep your bundle lean. No bloated dependencies dragging down your app's load time.
          </div>
        </div>

        <div className="text-center p-6">
          <div className="w-14 h-14 bg-muted rounded-xl flex items-center justify-center mx-auto mb-5">
            <Layers className="w-7 h-7" />
          </div>
          <h3 className="font-heading font-bold text-xl mb-3">Works With Every Provider</h3>
          <div className="text-muted-foreground">
            Swap between Blockfrost, Koios, Maestro, Ogmios, and UTxO RPC with one line. Same API, different backend. Run offline for testing with the built-in offline fetcher.
          </div>
        </div>
      </div>
    </div>
  </section>

  {/* ═══════════════ AI AGENT SKILLS ═══════════════ */}
  <FeatureSplit
    eyebrowIcon={<Sparkles className="w-4 h-4 mr-1" />}
    eyebrow="Agent Skills"
    title="AI-Powered Development"
    description="Give your AI coding assistant deep knowledge of Mesh SDK. Agent Skills work with Claude Code, Cursor, Codex, and 37+ tools so your AI already knows the API, patterns, and common errors."
    links={[
      { icon: BoltIcon, text: "Transaction building patterns", link: "https://github.com/MeshJS/skills/tree/main/mesh-transaction" },
      { icon: WalletIcon, text: "Wallet integration recipes", link: "https://github.com/MeshJS/skills/tree/main/mesh-wallet" },
      { icon: WrenchScrewdriverIcon, text: "Low-level utilities & troubleshooting", link: "https://github.com/MeshJS/skills/tree/main/mesh-core-cst" },
    ]}
    footerLink={{ href: "https://github.com/MeshJS/skills", text: "Browse on GitHub" }}
    codeLabel="terminal"
  >
    ```bash
      # Install all Mesh skills
      npx skills add MeshJS/skills

      # Install for a specific agent
      npx skills add MeshJS/skills -a claude-code
      npx skills add MeshJS/skills -a cursor
      npx skills add MeshJS/skills -a codex
    ```
  </FeatureSplit>
  {/* ═══════════════ TRANSACTION BUILDER (text left, code right) ═══════════════ */}
  <FeatureSplit
    eyebrowIcon={<Blocks className="w-4 h-4 mr-1" />}
    eyebrow="Transaction Builder"
    title="Cardano Transaction Builder in TypeScript"
    description="MeshTxBuilder is a chainable API for constructing any Cardano transaction. Build simple ADA transfers, Plutus script interactions, token minting, governance votes, and staking operations with automatic UTXO selection and fee calculation."
    links={[
      { icon: BoltIcon, text: "Send ADA, tokens & mint NFTs", link: "/apis/txbuilder/basics" },
      { icon: DocumentCheckIcon, text: "Plutus V1/V2/V3 script spending", link: "/apis/txbuilder/smart-contracts" },
      { icon: CloudIcon, text: "Governance, staking & pool ops", link: "/apis/txbuilder/staking" },
    ]}
    codeLabel="sendAda.ts"
  >
    ```typescript
      import { MeshTxBuilder } from '@meshsdk/transaction';

      const txBuilder = new MeshTxBuilder();

      const unsignedTx = await txBuilder
        .txIn(txHash, txIndex)
        .txOut(recipientAddress, [
          { unit: 'lovelace', quantity: '5000000' },
        ])
        .changeAddress(senderAddress)
        .selectUtxosFrom(utxos)
        .complete();
    ```
  </FeatureSplit>

  {/* ═══════════════ REACT COMPONENTS (code LEFT, text RIGHT - alternated) ═══════════════ */}
  <FeatureSplit
    reverse
    eyebrowIcon={<img className="w-4 h-4 mr-1" src="/icons/react.svg" alt="react-logo" />}
    eyebrow="React Components"
    title="Cardano React Components and Wallet Hooks"
    description="Drop in pre-built React components for Cardano wallet connections and use hooks to read balances, addresses, and assets. Works with Next.js, Vite, and any React setup."
    links={[
      { icon: RocketLaunchIcon, text: "Getting Started with React", link: "/react/getting-started" },
      { icon: PaintBrushIcon, text: "UI Components", link: "/react/ui-components" },
      { icon: BoltIcon, text: "Wallet Hooks", link: "/react/wallet-hooks" },
    ]}
    codeLabel="connectWallet.tsx"
  >
    ```tsx
      import { CardanoWallet } from '@meshsdk/react';

      export default function Page() {
        return (
          <>
            <CardanoWallet
              label={"Connect a Wallet"}
              persist={true}
              onConnected={()=>{console.log('on connected')}}
            />
          </>
        );
      }
    ```
  </FeatureSplit>

  {/* ═══════════════ WALLET INTEGRATION (text left, code right + sub-links) ═══════════════ */}
  <FeatureSplit
    eyebrowIcon={<WalletIcon className="w-4 h-4 mr-1" />}
    eyebrow="Wallet Integration"
    title="Cardano Wallet Integration for Browser and Server"
    description="Connect CIP-30 browser wallets like Nami, Eternl, and Lace, or create server-side wallets with seed phrases and private keys. Build multi-signature apps, minting backends, and automated scripts with one unified API."
    links={[
      { icon: ComputerDesktopIcon, text: "Browser Wallet (CIP-30)", link: "/apis/wallets/browserwallet" },
      { icon: WrenchScrewdriverIcon, text: "Headless Wallet (Server-Side)", link: "/apis/wallets/meshwallet" },
      { icon: BoltIcon, text: "Prove Wallet Ownership", link: "/guides/prove-wallet-ownership" },
    ]}
    codeLabel="meshWallet.tsx"
    codeClassName="wrap-anywhere"
  >
    ```tsx
      import { MeshWallet } from '@meshsdk/core';

      const wallet = new MeshWallet({
        networkId: 0, // 0: testnet, 1: mainnet
        fetcher: provider,
        submitter: provider,
        key: {
          type: 'mnemonic',
          words: ["solution","solution",...,"solution"],
        },
      });
    ```
  </FeatureSplit>

  {/* ═══════════════ SMART CONTRACTS (code LEFT, text RIGHT - alternated) ═══════════════ */}
  <section className="py-20 lg:py-28">
    <div className="container mx-auto px-4 sm:px-6 lg:px-8">
      <div className="grid lg:grid-cols-2 gap-12 items-center">
        <div className="order-2 lg:order-1 grid grid-cols-2 sm:grid-cols-3 gap-3">
          {[
            { icon: ShoppingCart, text: "Marketplace", link: "/smart-contracts/marketplace" },
            { icon: Lock, text: "Escrow", link: "/smart-contracts/escrow" },
            { icon: Coins, text: "Vesting", link: "/smart-contracts/vesting" },
            { icon: ArrowLeftRight, text: "Swap", link: "/smart-contracts/swap" },
            { icon: Gift, text: "Giftcard", link: "/smart-contracts/giftcard" },
            { icon: Database, text: "NFT Minting", link: "/smart-contracts/plutus-nft" },
          ].map((item, index) => (
            <Link key={index} href={item.link}>
              <Card className="h-full hover:border-foreground/20 transition-colors duration-150">
                <CardHeader className="p-4 text-center">
                  <item.icon className="w-6 h-6 mx-auto mb-2" />
                  <CardTitle className="text-sm font-medium">{item.text}</CardTitle>
                </CardHeader>
              </Card>
            </Link>
          ))}
        </div>

        <div className="order-1 lg:order-2">
          <div className="inline-flex items-center mb-4 px-3 py-1 rounded-full border border-border text-sm font-medium">
            <DocumentCheckIcon className="w-4 h-4 mr-1" />
            Smart Contracts
          </div>

          <h2 className="font-heading font-bold text-3xl lg:text-4xl mb-6">
            Production-Ready Smart Contracts
          </h2>
          <div className="text-lg mb-6 text-muted-foreground">
            Skip writing boilerplate. Mesh ships open-source smart contracts for common use cases: NFT marketplaces, token vesting, escrow, asset swaps, payment splitting, and more. Each contract comes with full TypeScript SDK integration, documentation, and live demos.
          </div>
          <div className="space-y-3">
            {[
              { icon: BoltIcon, text: "Aiken smart contract support", link: "/aiken" },
              { icon: DocumentCheckIcon, text: "Plutus V1/V2/V3 validators", link: "/smart-contracts" },
              { icon: WrenchScrewdriverIcon, text: "CIP-57 blueprint generation", link: "/apis/utilities/blueprints" },
            ].map((item, index) => (
              <Link
                key={index}
                className="flex items-center space-x-3 text-muted-foreground hover:text-foreground transition-colors duration-150"
                href={item.link}
              >
                <item.icon className="w-4 h-4" />
                <span>{item.text}</span>
              </Link>
            ))}
          </div>
        </div>
      </div>
    </div>
  </section>

  {/* ═══════════════ 3-STEP QUICKSTART + CTA ═══════════════ */}
  <section className="py-20 lg:py-28">
    <div className="container mx-auto px-4 sm:px-6 lg:px-8">
      <div className="text-center mb-16">
        <h2 className="font-heading font-bold text-3xl lg:text-4xl mb-4">Start Building in Three Steps</h2>
      </div>

      <div className="grid md:grid-cols-3 gap-8 max-w-4xl mx-auto mb-16">
        <div className="text-center">
          <div className="w-10 h-10 rounded-full bg-primary text-primary-foreground flex items-center justify-center mx-auto mb-4 text-lg font-bold">1</div>
          <h3 className="font-heading font-bold text-lg mb-2">Install</h3>
          <code className="text-sm bg-fd-card border border-border rounded-lg px-3 py-1.5 font-mono">npm i @meshsdk/core</code>
        </div>

        <div className="text-center">
          <div className="w-10 h-10 rounded-full bg-primary text-primary-foreground flex items-center justify-center mx-auto mb-4 text-lg font-bold">2</div>
          <h3 className="font-heading font-bold text-lg mb-2">Pick a Provider</h3>
          <p className="text-sm text-muted-foreground">Blockfrost, Koios, Maestro, Ogmios, or offline</p>
        </div>

        <div className="text-center">
          <div className="w-10 h-10 rounded-full bg-primary text-primary-foreground flex items-center justify-center mx-auto mb-4 text-lg font-bold">3</div>
          <h3 className="font-heading font-bold text-lg mb-2">Build</h3>
          <p className="text-sm text-muted-foreground">Transactions, wallets, minting, smart contracts</p>
        </div>
      </div>

      <div className="flex flex-col sm:flex-row gap-4 justify-center items-center">
        <Link
          href="/guides/nextjs"
          className={cn(
            buttonVariants({ size: 'lg', color: 'primary' }),
            'rounded-xl px-10 py-4 text-lg font-semibold flex items-center justify-center gap-2'
          )}
        >
          Build Your First dApp
          <ArrowRight className="w-5 h-5" />
        </Link>

        <Link
          href="https://github.com/MeshJS/examples"
          className={cn(
            buttonVariants({ size: 'lg', color: 'secondary' }),
            'rounded-xl px-10 py-4 text-lg font-semibold flex items-center justify-center gap-2'
          )}
        >
          <Code className="w-5 h-5" />
          Clone an Example
        </Link>
      </div>
    </div>
  </section>
</main>
